{
  "name": "ghost-convertkit-integration",
  "main": "src/index.js",
  "scripts": {
    "start": "serverless offline",
    "deploy": "serverless deploy function -f convertkit-ghost --stage production",
    "prettier": "prettier -w src",
    "lint": "eslint src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "eslint-config-prettier": "^9.1.0",
    "prettier": "^3.1.1",
    "serverless": "^3.38.0"
  },
  "dependencies": {
    "@tryghost/admin-api": "^1.13.11",
    "axios": "^1.6.5",
    "zod": "^3.22.4"
  }
}
